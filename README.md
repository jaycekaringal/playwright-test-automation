# Playwright TypeScript Automation

This project automates the Practice Software Testing demo site with Playwright and TypeScript.

## Setup

1. Install dependencies: `npm install`
2. Install browsers: `npx playwright install chromium`
3. Review the `.env` file for the documented default accounts.

## Run tests

- `npm test`
- `npx playwright test --project=chromium`
